<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    style="@style/Studdy.Card.Flat"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="16dp"
    app:strokeColor="@color/stroke_light"
    app:strokeWidth="1dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- Question Header (Text & Status Icon) -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginBottom="12dp">

            <ImageView
                android:id="@+id/iv_question_status"
                android:layout_width="20dp"
                android:layout_height="20dp"
                android:layout_marginEnd="8dp"
                android:contentDescription="Status"
                tools:src="@drawable/ic_check" />

            <TextView
                android:id="@+id/tv_review_question_text"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:fontFamily="sans-serif-medium"
                android:textColor="@color/text_primary"
                android:textSize="15sp"
                tools:text="Q1. What is the derivative of x^2?" />
        </LinearLayout>

        <!-- Options lists with custom indicator coloring -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <!-- Option 0 -->
            <TextView
                android:id="@+id/tv_review_option_0"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginBottom="6dp"
                android:padding="10dp"
                android:background="@drawable/bg_circle_primary_light"
                android:textColor="@color/text_primary"
                android:textSize="13sp"
                tools:text="A) x" />

            <!-- Option 1 -->
            <TextView
                android:id="@+id/tv_review_option_1"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginBottom="6dp"
                android:padding="10dp"
                android:background="@drawable/bg_circle_primary_light"
                android:textColor="@color/text_primary"
                android:textSize="13sp"
                tools:text="B) 2x" />

            <!-- Option 2 -->
            <TextView
                android:id="@+id/tv_review_option_2"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginBottom="6dp"
                android:padding="10dp"
                android:background="@drawable/bg_circle_primary_light"
                android:textColor="@color/text_primary"
                android:textSize="13sp"
                tools:text="C) 2" />

            <!-- Option 3 -->
            <TextView
                android:id="@+id/tv_review_option_3"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:padding="10dp"
                android:background="@drawable/bg_circle_primary_light"
                android:textColor="@color/text_primary"
                android:textSize="13sp"
                tools:text="D) x^3" />

        </LinearLayout>

    </LinearLayout>
</com.google.android.material.card.MaterialCardView>
